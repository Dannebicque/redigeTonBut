{% extends 'base.html.twig' %}

{% block header %}Import{% endblock %}

{% block content %}
    <section class="scroll-section" id="buttonTags">
        <h2 class="small-title">Import tableau référentiel de formation (béta)</h2>
        <div class="card mb-5">
            <div class="card-body">
                <form method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="typeFichierb1">Type de fichier</label>
                        <div class="form-check form-check-inline">
                            <input class="form-check-input" type="radio" name="typeFichierPartiel" id="typeFichierb1"
                                   value="excel" checked>
                            <label class="form-check-label" for="typeFichierb1">Modèle Excel</label>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="fichier">Fichier</label>
                        <input class="form-control" type="file" name="fichier" id="fichier">
                    </div>
                    <br>
                    <div class="alert alert-warning">
                        Le fichier excel doit respecter <a href="{{ asset('documents_gt/modele-import-referentiel-formation.xlsx') }}">
                            le modèle que vous pouvez télécharger ici</a>. Les
                        codes des apprentissages critiques doivent correspondre à ceux d'ORéBUT.
                        <a href="{{ path('administration_apc_apprentissage_critique_index', {departement: user_data
                        .departement.id})
                        }}" target="_blank">Vous pouvez les retrouver ici</a>.
                        Les codes parcours de votre spécialité sont :
                        <ul>
                            {% for parcour in parcours %}
                            <li><strong>{{ parcour.code }}</strong> pour le parcours "{{ parcour.libelle }}"</li>
                            {% endfor %}
                        </ul>
                    </div>

                    <button type="submit" class="btn btn-success">Importer</button>
                </form>

            </div>
        </div>
    </section>
{% endblock %}

