{% extends 'pdf/basepdf.html.twig' %}

{% block titre %}
    Tableau Croisé semestre {{ semestre.ordreLmd }} {% if parcours != null %} | {{ parcours.libelle }}{% endif %}| {{ departement.libelle }}
{% endblock %}

{% block styles %}
    @page { size: landscape; }
{% endblock %}

{% block content %}
    {#    <h1>Tableau croisé pour le B.U.T {{ departement.libelle }}, semestre {{ semestre.ordreLmd }}</h1> #}
    {#    {% if parcours != null %} #}
    {#        <h2>Parcours : {{ parcours.libelle }}</h2> #}
    {#    {% endif %} #}
    <table class="table">
        <thead>
        <tr>
            <th></th>
            <th>AC</th>
            {% for sae in saes %}
                <th class="tdsae">
                    {% if saeImages[sae.id] is defined %}
                        <img src="{{ saeImages[sae.id] }}">
                    {% else %}
                        <img src="{{ linuxpath|default('') }}{{ asset('latex/'~departement.numeroAnnexe~'/tableaux/sae_'~sae.id~'.png') }}">
                    {% endif %}
                </th>
            {% endfor %}

            {% for ressource in ressources %}
                <th>
                    {% if ressourceImages[ressource.id] is defined %}
                        <img src="{{ ressourceImages[ressource.id] }}">
                    {% else %}
                        <img src="{{ linuxpath|default('') }}{{ asset('latex/'~departement.numeroAnnexe~'/tableaux/ressource_'~ressource.id~'.png') }}">
                    {% endif %}
                </th>
            {% endfor %}
        </tr>
        </thead>
        <tbody>

        {% for niveau in niveaux %}
            <tr>
                <td rowspan="{{ niveau.apcApprentissageCritiques|length+1 }}" class="bg_{{ niveau.competence
                .couleur }}">
                    <div class="vertical">
                        <span class="nomcomp">{{ niveau.competence.nomCourt }}</span>
                    </div>
                </td>
            </tr>

            {% for ac in niveau.apcApprentissageCritiques %}
                <tr>
                    <td class="bg_{{ niveau.competence.couleur }}">
                        <p data-bs-toggle="tooltip" title="{{ ac.libelle }}">{{ ac.code }}</p>
                    </td>
                    {% for sae in saes %}
                        <td class="text-center tdsae">
                            {% if tab.saes[sae.id] is defined and tab.saes[sae.id][ac.id] is defined %}
                                <span class="croix">X</span>{% endif %}
                        </td>
                    {% endfor %}

                    {% for ressource in ressources %}
                        <td class="text-center">
                            {% if tab.ressources[ressource.id] is defined and tab.ressources[ressource.id][ac.id] is
                                defined %}<span class="croix">X</span>{% endif %}
                        </td>
                    {% endfor %}
                </tr>
            {% endfor %}
            <tr>
                <td colspan="{{ ressources|length + saes|length + 2 }}"></td>
            </tr>
        {% endfor %}
        {% set donneesSemestre = donnees[semestre.ordreLmd] ?? null %}
        <tr>
            <th colspan="2">Volume total</th>
            <td colspan="{{ saes|length }}" class="text-center tdsae">&nbsp;</td>
            {% for ressource in ressources %}
                <td class="text-center">
                    {{ ressource.heuresTotales }}
                </td>
            {% endfor %}
            <td class="text-center">{{ donneesSemestre is not null ? donneesSemestre.totalEnseignementRessources : '' }}</td>
        </tr>
        <tr>
            <th colspan="2">Dont TP</th>
            <td colspan="{{ saes|length }}" class="text-center tdsae">&nbsp;</td>
            {% for ressource in ressources %}
                <td class="text-center">
                    {{ ressource.tpPpn }}
                </td>
            {% endfor %}
            <td class="text-center">{{ donneesSemestre is not null ? donneesSemestre.totalDontTpRessources : '' }}</td>
        </tr>

        <tr>
            <th colspan="2">Adaptation Locale (SAÉ)</th>
            <td colspan="{{ saes|length }}" class="text-center tdsae">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeuresEnseignementSae : '' }}</td>
            <td class="text-center" colspan="{{ ressources|length }}">
                &nbsp;
            </td>
            <td class="text-center">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeuresEnseignementSae : '' }}</td>
        </tr>
        <tr>
            <th colspan="2">Adaptation Locale <br>(Ressources ou SAÉ)</th>
            <td colspan="{{ saes|length + ressources|length }}" class="text-center">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeureeEnseignementSaeRessource : '' }}</td>
            <td class="text-center">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeureeEnseignementSaeRessource : '' }}</td>
        </tr>
        <tr>
            <th colspan="2">TP Adaptation locale</th>
            <td colspan="{{ saes|length + ressources|length }}" class="text-center">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeuresDontTpSaeRessource : '' }}</td>
            <td class="text-center">{{ donneesSemestre is not null ? donneesSemestre.vhNbHeuresDontTpSaeRessource : '' }}</td>
        </tr>
        </tbody>
    </table>
{% endblock %}
